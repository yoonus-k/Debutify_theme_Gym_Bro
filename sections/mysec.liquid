{% for item in product.images%}
{%- assign variable = {{section.settings.range}}-%}
{{section.settings.range}}
  <img src="{{item|image_url:width:100}}">
{% endfor %}
{% schema %}
{
   "name": "mysec",
  "class": "mysec",
  "settings": [
    {
       "type": "color",
      "id": "select_color",
      "label":"mysec settings"
         },{
          "type":"range",
          "id":"range",
          "label":"choase a widh",
          "min":1,
          "max":100,
          "step":1,
          "default":1
         }
  ],
 "presets": [
    {
        "category": "reference",
        "name":"mysec"
    }
 ]
}
{% endschema %}